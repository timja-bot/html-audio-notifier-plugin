package jenkins.plugins.htmlaudio.domain;

import static org.junit.Assert.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;


@RunWith(JUnit4.class)
public class VolatileNotificationRepoAndFactoryStressTest {
    
    @Test
    public void repo_maintains_integrity_during_onslaught_of_crazy_monkeys() {
        
        // TODO fire up a bunch of threads, create/list/remove go nuts!
            // store results in a list & check 'em after the threads are all done??
        
        // .. make sure that it's still stable after all this
        
        fail();
    }
}
