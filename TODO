	
    - acceptance test for plugin, fail a job + invoke /next etc..
    - package cleanup
    - long polling, configurable or maybe as default?
    - the cleanup-service must be invoked by a timer, possible memory leak if no js-clients are polling..
    - css forgery (.crumb) applies to all POST requests? Probably breaks the current javascript POSTing..
     